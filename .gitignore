* .tar.gz

* .exe
* .pdb
src / биткойн
src / bitcoind
src / bitcoin-cli
src / bitcoin-tx
src / биткойн-кошелек
src / test / fuzz
! src / test / fuzz / * . *
src / test / test_bitcoin
src / qt / test / test_bitcoin-qt

# autoreconf
Makefile.in
aclocal.m4
autom4te.cache /
build-aux / config.guess
build-aux / config.sub
build-aux / depcomp
build-aux / install-sh
build-aux / ltmain.sh
build-aux / m4 / libtool.m4
build-aux / m4 / lt ~ устаревший.m4
build-aux / m4 / ltoptions.m4
build-aux / m4 / ltsugar.m4
build-aux / m4 / ltversion.m4
build-aux / отсутствует
build-aux / compile
build-aux / test-driver
config.cache
config.log
config.status
настроить
Libtool
src / config / bitcoin-config.h
src / config / bitcoin-config.h.in
src / config / stamp-h1
src / obj
поделиться / setup.nsi
поделиться / qt / Info.plist

src / univalue / gen

src / qt / * .moc
src / qt / moc_ * .cpp
src / qt / forms / ui_ * .h

src / qt / test / moc * .cpp

src / qt / bitcoin-qt.config
src / qt / bitcoin-qt.creator
src / qt / bitcoin-qt.creator.user
src / qt / bitcoin-qt.files
src / qt / bitcoin-qt.includes

.deps
.dirstamp
.libs
, * .swp
* . * ~ *
* .бак
* .rej
* .orig
* .pyc
* .o
* .o- *
* .a
* .pb.cc
* .pb.h
* .dat

* .log
* .trs
* .dmg

* .json.h
* .raw.h

# Игнорировать только неожиданные патчи
* .patch
! зависит / патчи / ** / *. патч

# libtool объектные файлы
* .lo
* .la

# Компиляция и часть препроцессора Qt
* .qm
Makefile
! зависит / Makefile
Bitcoin-кварта
Bitcoin-Qt.app
background.tiff *

# Qt Creator
Makefile.am.user

# Юнит-тесты
Makefile.test
Bitcoin-qt_test

# Ресурсы cpp
qrc_ * .cpp

# Mac специфичен
.DS_Store
строить

# lcov
* .gcno
* .gcda
/ * .info
test_bitcoin.coverage /
total.coverage /
coverage_percent.txt

# строить тесты
линукс-покрытие-сборка
линукс-сборка
win32-сборка
test / config.ini
тест / кеш / *

! src / leveldb * / Makefile

/ doc / doxygen /

libbitcoinconsensus.pc
contrib / devtools / split-debug.sh

# Вывод из запущенной установки db4
дБ4 /

# clang-check
* .plist

osx_volname
dist /
* .background.tiff
